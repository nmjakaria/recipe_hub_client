export const metadata = {
  title: "Sign Up | Recipe Hub",
  description: "Create a verified account on Recipe Hub to start exploring and managing your favorite recipes instantly.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}