export const metadata = {
  title: "Manage Recipes | Admin Dashboard | Recipe Hub",
  description: "Review, approve, update, or remove recipes to ensure high-quality and community-compliant content on Recipe Hub.",
};
export default function ManageRecipeLayout({ children }) {
  return <>{children}</>;
}