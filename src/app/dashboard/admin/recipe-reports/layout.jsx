export const metadata = {
  title: "Recipe Reports | Recipe Hub",
  description: "Review user-submitted reports, track recipe analytics, or report inappropriate content to maintain quality on Recipe Hub.",
};
export default function RecipeReportLayout({ children }) {
  return <>{children}</>;
}